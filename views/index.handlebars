<h1>Burgers</h1>

<h2>Burgers that are not yet comsumed</h2>

<ul>
  {{#each burgers}}
   {{burger.toppings}}
  {{/each}}
</ul>

<h2>Burgers that are ready to be eaten.</h2>

{{!-- <ul>
  {{#each Burgers}}
    {{#if eaten}}
      {{> Burgers/Burger-block eat=false}}
    {{/if}}
  {{/each}}
</ul> --}}

<h2>Make a Burger</h2>
<form action="/burgers/create" method="POST">

  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="toppings">
  </div>

  {{!-- <div class="form-group">
    <label for="eat">Consumed?</label><br>
    <input type="radio" name="eaten" value="1" checked> Eaten!<br>
  </div> --}}

  <button type="submit" id="submit-burger">Make Burger</button>


  
</form>
