<h1>Burgers</h1>

<h2>Burgers that are not yet comsumed</h2>

<ul>
  {{#each Burgers}}
    {{#unless eaten}}
      {{> Burgers/Burger-block eat=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are ready to be eaten.</h2>

<ul>
  {{#each Burgers}}
    {{#if eaten}}
      {{> Burgers/Burger-block eat=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Make a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="eat">Consumed?</label><br>
    <input type="radio" name="eat" value="1" checked> Eaten!<br>
    <input type="radio" name="eat" value="0"> Ready
  </div>

  <button type="submit">Make Burger</button>
</form>
